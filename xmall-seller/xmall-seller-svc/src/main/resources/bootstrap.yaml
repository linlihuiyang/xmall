dubbo:
  scan:
    base-packages: com.xll.xmall.seller
  protocol:
    name: dubbo
    port: -1
  registry:
    address: spring-cloud://localhost

spring:
  application:
    name: xmall-seller-svc
  main:
    allow-bean-definition-overriding: true
  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848
